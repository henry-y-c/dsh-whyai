# 安全策略

## 支持范围

项目入口为 [GitHub 仓库](https://github.com/henry-y-c/dsh-whyai) 与 [npm 包](https://www.npmjs.com/package/dsh-whyai)。安全修复默认只进入最新受支持版本；具体范围在本文件和变更记录中同步更新。

| 版本 | 状态 |
|---|---|
| 0.3.1 | 接受安全报告，不承诺生产稳定性 |

## 报告漏洞

请不要通过公开 Issue 报告尚未修复的漏洞，也不要附带真实 Token、Cookie、凭据文件或私密咨询正文。

已启用 [GitHub 私密漏洞报告](https://github.com/henry-y-c/dsh-whyai/security/advisories/new)，请优先使用该入口。若入口不可用，可在 [Issues](https://github.com/henry-y-c/dsh-whyai/issues) 仅请求维护者提供私密渠道，不附漏洞细节或敏感材料。不要把公开 Issue 当作私密报告渠道。

报告建议包含：

- 受影响版本或 commit；
- 复现条件和最小复现步骤；
- 实际影响；
- 是否涉及数据外发、凭据、计费或子进程逃逸；
- 不含真实秘密的日志或测试夹具。

维护者确认问题后会协调修复、回归测试、版本发布和披露时间。尚未确认前，不承诺具体响应时限。

## 重点安全边界

本项目尤其关注：

- 咨询正文或凭据进入 argv、日志、错误信息和测试产物；
- `requireApproval: true` 时绕过 create/send 的用户批准，或在默认关闭时错误覆盖 DSH 上游权限决策；
- 自动读取或上传本地文件；
- 原始 stderr、服务端响应、推理内容或工具执行记录泄漏；
- 超时、取消或卸载后的孤儿进程；
- CLI 协议漂移导致身份错配、静默截断或错误状态被当成成功；
- 恶意 Partner 内容被当作可信指令执行。

## 管理操作的信任边界（未发布）

侧栏安装、登录和退出作用于 **DSH 服务所在机器及其运行用户**，不一定是浏览器所在机器。它们共享该用户的 WhyAI CLI 身份；本插件不提供按浏览器用户隔离的 WhyAI 账号。仅应向有权管理该宿主账号的用户开放 DSH Web 会话。

安装会下载并执行 WhyAI 官方 HTTPS 地址提供的安装器，属于对官方服务和其后续下载链的信任，不是插件独立完成的签名或固定哈希验证。取消或客户端断线也不保证撤销已经写入的安装文件、授权或远端注销；需要按最终操作状态和后续查询判断结果。

仅报告 WhyAI 服务自身的问题时，应同时遵循 WhyAI 官方安全报告渠道；本仓库只负责插件代码及其集成行为。
